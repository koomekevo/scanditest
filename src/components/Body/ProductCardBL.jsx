import React, { Component } from 'react';
// eslint-disable-next-line no-unused-vars
import styled from 'styled-components';

export default class ProductCardBL extends Component {
  render() {
    return (
      <div />
    );
  }
}
